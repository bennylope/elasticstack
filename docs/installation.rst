============
Installation
============

At the command line::

    $ easy_install elasticstack

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv elasticstack
    $ pip install elasticstack